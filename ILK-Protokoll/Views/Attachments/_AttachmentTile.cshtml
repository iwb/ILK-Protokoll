@using ILK_Protokoll.Controllers
@model ILK_Protokoll.Models.Document
@{
	string iconname = Model.Extension ?? "default";
	if (!((HashSet<string>) ViewBag.KnownExtensions).Contains(iconname))
	{
		iconname = "default";
	}
}<a href="@Html.Action("DownloadNewest", "Attachments", new {id = Model.Guid})" target="_blank" class="btn btn-default">
	<img src="~/img/fileicons/@(iconname).png" height="16" />
	@Model.DisplayName
</a>