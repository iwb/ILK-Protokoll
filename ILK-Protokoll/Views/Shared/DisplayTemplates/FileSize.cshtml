@model int
@{
	string[] prefixArray = {"Byte", "KB", "MB", "GB"};
	int magnitude = (int)Math.Log(Model, 1000);
	var suffix = prefixArray[magnitude];
	var result = string.Format("{0:0}{1}{2}", Model / Math.Pow(1000, magnitude), '\xA0', suffix);
}
<filesize bytes="@Model">@result</filesize>