@using ILK_Protokoll.util
@model IEnumerable<ILK_Protokoll.Models.Decision>

@{
	ViewBag.Title = "Beschlüsse";
}

<h2>@ViewBag.Title</h2>

<table class="table table-striped table-hover table-sortable">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.OriginTopic)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Type)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Text)
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.OriginTopic.Title)
				</td>
				<td>
					@item.Type.DisplayName()
				</td>
				<td>
					@item.Text.Shorten(200)
				</td>
				<td>
					@Html.ActionLink("Diskussion", "Details", "Topics", new { id = item.OriginTopic.ID }, null)
				</td>
			</tr>
		}
	</tbody>
</table>
