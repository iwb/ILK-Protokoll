@using ILK_Protokoll.util
@model IEnumerable<ILK_Protokoll.Areas.Session.Models.Lists.EmployeePresentation>

@{
	string tableclass = ViewBag.Reporting ? null : "table-responsive";
}

<div class="panel panel-primary">
	<div class="panel-heading panel-collapse-heading" style="position: relative;">
		<h3 class="panel-title">
			<a data-toggle="collapse" data-target="#employeePresentation_table_container">
				Mitarbeiterpräsentationen
				<span class="divlink"></span>
			</a>
		</h3>
	</div>
	<div class="panel-collapse collapse in @tableclass" id="employeePresentation_table_container">
		<table id="employeePresentation_table" class="table table-striped table-hover table-sortable table-condensed">
			<thead>
				<tr>
					<th class="col-sm-2">
						@Html.DisplayNameFor(model => model.Employee)
					</th>
					<th class="col-sm-2">
						@Html.DisplayNameFor(model => model.Ilk)
					</th>
					<th class="col-sm-2">
						@Html.DisplayNameFor(model => model.Prof)
					</th>
					<th class="col-sm-2">
						@Html.DisplayNameFor(model => model.LastPresentation)
					</th>
					<th class="col-sm-2">
						@Html.DisplayNameFor(model => model.Documents)
					</th>
					<th class="col-sm-1">
						@Html.DisplayNameFor(model => model.Selected)
					</th>
					@if (!ViewBag.Reporting)
					{
						<th class="col-sm-1"></th>
					}
				</tr>
			</thead>
			<tbody id="employeePresentation_tbody">
				@foreach (var item in Model)
				{
					Html.RenderPartial("_Row", item);
				}
			</tbody>
			<tfoot>
				@if (!ViewBag.Reporting)
				{
					@Html.Action("_CreateForm", "LEmployeePresentations")
				}
			</tfoot>
		</table>
	</div>
</div>

@using (Html.Delayed(null, "List_Utilities"))
{
	<script type="text/javascript" src="~/Scripts/ListUtilities.js"></script>
}