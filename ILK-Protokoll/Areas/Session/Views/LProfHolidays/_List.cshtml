@using ILK_Protokoll.util
@model IEnumerable<ILK_Protokoll.Areas.Session.Models.Lists.ProfHoliday>

<h3>Professorenurlaub</h3>

<table id="profHoliday_table" class="table table-striped table-hover table-sortable">
	<thead>
		<tr>
			<th class="col-sm-3">
				@Html.DisplayNameFor(model => model.Professor)
			</th>
			<th class="col-sm-3">
				@Html.DisplayNameFor(model => model.Occasion)
			</th>
			<th class="col-sm-5">
				Zeitraum
			</th>
			<th class="col-sm-1"></th>
		</tr>
	</thead>
	<tbody id="profHoliday_tbody">
		@foreach (var item in Model)
		{
			Html.RenderPartial("_Row", item);
		}
	</tbody>
<tfoot>
	@if (!ViewBag.Reporting)
	{
		@Html.Action("_CreateForm", "LProfHolidays")
	}
	</tfoot>
</table>

@using (Html.Delayed(null, "List_Utilities"))
{
	<script type="text/javascript" src="~/Scripts/ListUtilities.js"></script>
}